name: Build MonitorIA Agent

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "agent/**"
      - "app/api/agent/cameras/**"
      - "app/api/agent/config/**"
      - "src/lib/agent-camera.ts"
      - ".github/workflows/build-agent.yml"

permissions:
  contents: read

jobs:
  build-windows:
    name: Windows x64 baseline
    runs-on: windows-latest
    timeout-minutes: 20

    defaults:
      run:
        working-directory: agent

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Instalar Bun baseline 1.3.14
        uses: oven-sh/setup-bun@v2
        with:
          bun-download-url: "https://github.com/oven-sh/bun/releases/download/bun-v1.3.14/bun-windows-x64-baseline.zip"
          no-cache: true

      - name: Confirmar runtime baseline
        shell: pwsh
        run: |
          bun --version
          bun --revision
          $bunPath = (Get-Command bun).Source
          Write-Host "Runtime Bun: $bunPath"

      - name: Instalar dependências
        run: bun install --frozen-lockfile

      - name: Confirmar código-fonte do Agent
        shell: pwsh
        run: |
          if (-not (Test-Path "src/index.ts")) {
            throw "agent/src/index.ts não foi encontrado no checkout."
          }

          $sourceFiles = Get-ChildItem src -Filter *.ts
          if ($sourceFiles.Count -lt 11) {
            throw "Código-fonte incompleto: foram encontrados apenas $($sourceFiles.Count) arquivos TypeScript."
          }

          $sourceFiles | Select-Object Name

      - name: Verificar TypeScript do Agent
        run: bun run check

      - name: Gerar executável com o runtime instalado
        run: bun run build:win

      - name: Validar executável
        shell: pwsh
        run: |
          $exe = Resolve-Path "dist/monitoria-agent.exe"
          $size = (Get-Item $exe).Length

          if ($size -lt 1000000) {
            throw "O executável gerado parece incompleto: $size bytes."
          }

          Write-Host "Executável: $exe"
          Write-Host "Tamanho: $size bytes"

          & $exe self-test
          if ($LASTEXITCODE -ne 0) {
            throw "O autoteste do executável falhou."
          }

          & $exe reset
          if ($LASTEXITCODE -ne 0) {
            throw "O comando reset falhou."
          }

      - name: Publicar artefato
        uses: actions/upload-artifact@v4
        with:
          name: monitoria-agent-windows-x64-baseline-v0.7.2
          path: agent/dist/monitoria-agent.exe
          if-no-files-found: error
          retention-days: 30
