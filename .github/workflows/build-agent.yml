name: Build MonitorIA Agent

on:
  workflow_dispatch:
  # O filtro "paths" foi removido de propósito. Com branches e tags no mesmo
  # gatilho, o filtro de caminho valeria também para as tags, e uma tag de
  # release sem mudança em agent/ não dispararia a publicação.
  push:
    branches:
      - main
    tags:
      - "agent-v*"

permissions:
  contents: write

env:
  AGENT_VERSION: "0.10.0"

  # Dependências fixadas por hash. Nada de "latest": um binário que muda
  # sozinho muda o hash do instalador, e a reputação do SmartScreen recomeça
  # do zero a cada troca silenciosa.
  FFMPEG_URL: "https://github.com/BtbN/FFmpeg-Builds/releases/download/autobuild-2026-08-01-13-21/ffmpeg-n8.1.2-34-g9b6c8969e0-win64-lgpl-shared-8.1.zip"
  FFMPEG_SHA256: "3bba81dcfd017a6ea1627905549769913948831ef10f3e7df7541f736067bff8"
  WINSW_URL: "https://github.com/winsw/winsw/releases/download/v2.12.0/WinSW.NET461.exe"
  WINSW_SHA256: "b5066b7bbdfba1293e5d15cda3caaea88fbeab35bd5b38c41c913d492aadfc4f"

jobs:
  build-windows:
    name: Instalador Windows x64
    runs-on: windows-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Instalar Bun baseline 1.3.14
        uses: oven-sh/setup-bun@v2
        with:
          bun-download-url: "https://github.com/oven-sh/bun/releases/download/bun-v1.3.14/bun-windows-x64-baseline.zip"
          no-cache: true

      - name: Instalar dependências
        working-directory: agent
        run: bun install --frozen-lockfile

      - name: Verificar TypeScript
        working-directory: agent
        run: bun run check

      - name: Gerar executável do Agent
        working-directory: agent
        run: bun run build:win

      - name: Validar executável
        working-directory: agent
        shell: pwsh
        run: |
          $exe = Resolve-Path "dist/monitoria-agent.exe"
          if ((Get-Item $exe).Length -lt 1000000) {
            throw "Executável incompleto."
          }
          & $exe self-test
          if ($LASTEXITCODE -ne 0) { throw "O autoteste falhou." }

      # ------------------------------------------------------ dependências

      - name: Baixar e conferir FFmpeg
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build/ffmpeg | Out-Null
          Invoke-WebRequest -Uri $env:FFMPEG_URL -OutFile ffmpeg.zip

          $hash = (Get-FileHash ffmpeg.zip -Algorithm SHA256).Hash.ToLower()
          if ($hash -ne $env:FFMPEG_SHA256) {
            throw "SHA256 do FFmpeg divergente. Esperado $env:FFMPEG_SHA256, obtido $hash."
          }

          Expand-Archive ffmpeg.zip -DestinationPath ffmpeg-tmp
          # Build shared: ffmpeg.exe e ffprobe.exe compartilham as DLLs. Com
          # os dois binários exigidos pela diretriz de descoberta, shared
          # ocupa 43 MB comprimidos contra 66 MB de duas builds estáticas.
          # ffplay não é usado e fica de fora.
          foreach ($name in @("ffmpeg.exe", "ffprobe.exe")) {
            $source = Get-ChildItem ffmpeg-tmp -Recurse -Filter $name | Select-Object -First 1
            if (-not $source) { throw "$name não encontrado no pacote do FFmpeg." }
            Copy-Item $source.FullName "build/ffmpeg/$name"
          }

          Get-ChildItem ffmpeg-tmp -Recurse -Filter *.dll |
            ForEach-Object { Copy-Item $_.FullName "build/ffmpeg/$($_.Name)" }
          $license = Get-ChildItem ffmpeg-tmp -Recurse -Filter LICENSE.txt | Select-Object -First 1
          Copy-Item $license.FullName build/ffmpeg/LICENSE.txt

          # Registro da origem exata, para atender à obrigação de indicar o
          # fonte correspondente do binário LGPL redistribuído.
          @"
          FFmpeg redistribuído sem modificação.

          Origem : $env:FFMPEG_URL
          SHA256 : $env:FFMPEG_SHA256
          Licença: LGPL (build sem componentes GPL)
          Fonte  : https://github.com/BtbN/FFmpeg-Builds
          Upstream: https://git.ffmpeg.org/ffmpeg.git
          "@ | Out-File -Encoding utf8 build/ffmpeg/FFMPEG-ORIGEM.txt

      - name: Baixar e conferir WinSW
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri $env:WINSW_URL -OutFile build/monitoria-service.exe

          $hash = (Get-FileHash build/monitoria-service.exe -Algorithm SHA256).Hash.ToLower()
          if ($hash -ne $env:WINSW_SHA256) {
            throw "SHA256 do WinSW divergente. Esperado $env:WINSW_SHA256, obtido $hash."
          }

      # -------------------------------------------------------- assinatura
      #
      # Os quatro segredos vêm do eSigner da SSL.com. Enquanto não existirem,
      # todos os passos de assinatura são pulados e o build sai sem assinar,
      # o que serve para testar na própria loja. No dia em que os segredos
      # forem cadastrados, a assinatura passa a acontecer sozinha.

      - name: Verificar credenciais de assinatura
        id: signing
        shell: pwsh
        env:
          SSL_COM_USERNAME: ${{ secrets.SSL_COM_USERNAME }}
        run: |
          $enabled = -not [string]::IsNullOrWhiteSpace($env:SSL_COM_USERNAME)
          "enabled=$($enabled.ToString().ToLower())" | Out-File -Append $env:GITHUB_OUTPUT
          if (-not $enabled) {
            Write-Warning "Sem credenciais de assinatura. O build sairá sem assinar e NÃO deve ser distribuído."
          }

      # Cada binário é assinado individualmente. Antivírus inspecionam os
      # componentes, não só o instalador.
      - name: Assinar binários
        if: steps.signing.outputs.enabled == 'true'
        uses: sslcom/esigner-codesign@v1.3.2
        with:
          command: batch_sign
          username: ${{ secrets.SSL_COM_USERNAME }}
          password: ${{ secrets.SSL_COM_PASSWORD }}
          credential_id: ${{ secrets.SSL_COM_CREDENTIAL_ID }}
          totp_secret: ${{ secrets.SSL_COM_TOTP_SECRET }}
          dir_path: ${{ github.workspace }}\build
          output_path: ${{ github.workspace }}\build
          malware_block: false
          environment_name: PROD

      - name: Assinar o executável do Agent
        if: steps.signing.outputs.enabled == 'true'
        uses: sslcom/esigner-codesign@v1.3.2
        with:
          command: sign
          username: ${{ secrets.SSL_COM_USERNAME }}
          password: ${{ secrets.SSL_COM_PASSWORD }}
          credential_id: ${{ secrets.SSL_COM_CREDENTIAL_ID }}
          totp_secret: ${{ secrets.SSL_COM_TOTP_SECRET }}
          file_path: ${{ github.workspace }}\agent\dist\monitoria-agent.exe
          output_path: ${{ github.workspace }}\agent\dist
          malware_block: false
          environment_name: PROD

      # ------------------------------------------------------- instalador

      # O runner do Windows já traz o Inno Setup. Fixar versão fazia o
      # Chocolatey falhar por recusar rebaixar uma instalação mais nova.
      # Aqui apenas localizamos o compilador, e só instalamos se faltar.
      - name: Localizar o Inno Setup
        id: inno
        shell: pwsh
        run: |
          $candidatos = @(
            "C:\Program Files (x86)\Inno Setup 6\ISCC.exe",
            "C:\Program Files\Inno Setup 6\ISCC.exe"
          )

          $iscc = $candidatos | Where-Object { Test-Path $_ } | Select-Object -First 1

          if (-not $iscc) {
            Write-Host "Inno Setup não encontrado na imagem. Instalando..."
            choco install innosetup -y --no-progress
            $iscc = $candidatos | Where-Object { Test-Path $_ } | Select-Object -First 1
          }

          if (-not $iscc) { throw "Não foi possível localizar o ISCC.exe." }

          Write-Host "Usando: $iscc"
          & $iscc /? | Select-Object -First 1
          "path=$iscc" | Out-File -Append $env:GITHUB_OUTPUT

      - name: Compilar o instalador
        shell: pwsh
        run: |
          & "${{ steps.inno.outputs.path }}" "/DAppVersion=$env:AGENT_VERSION" "installer\monitoria.iss"
          if ($LASTEXITCODE -ne 0) { throw "Falha ao compilar o instalador." }

      - name: Assinar o instalador
        if: steps.signing.outputs.enabled == 'true'
        uses: sslcom/esigner-codesign@v1.3.2
        with:
          command: sign
          username: ${{ secrets.SSL_COM_USERNAME }}
          password: ${{ secrets.SSL_COM_PASSWORD }}
          credential_id: ${{ secrets.SSL_COM_CREDENTIAL_ID }}
          totp_secret: ${{ secrets.SSL_COM_TOTP_SECRET }}
          file_path: ${{ github.workspace }}\dist\MonitorIA-Setup.exe
          output_path: ${{ github.workspace }}\dist
          malware_block: false
          environment_name: PROD

      - name: Resumo do build
        shell: pwsh
        run: |
          $setup = Get-Item dist\MonitorIA-Setup.exe
          $mb = [math]::Round($setup.Length / 1MB, 1)
          $sig = (Get-AuthenticodeSignature $setup.FullName).Status

          Write-Host "Instalador: $mb MB"
          Write-Host "Assinatura: $sig"
          "Instalador: **$mb MB** · assinatura: **$sig**" | Out-File -Append $env:GITHUB_STEP_SUMMARY

      - name: Publicar artefato
        uses: actions/upload-artifact@v4
        with:
          name: MonitorIA-Setup-${{ env.AGENT_VERSION }}
          path: dist/MonitorIA-Setup.exe
          if-no-files-found: error
          retention-days: 30

      # A release só é criada em tag. O nome do arquivo é sempre o mesmo,
      # para a URL /releases/latest/download/MonitorIA-Setup.exe permanecer
      # estável e a reputação do SmartScreen se acumular num alvo só.
      - name: Publicar release
        if: startsWith(github.ref, 'refs/tags/agent-v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/MonitorIA-Setup.exe
          fail_on_unmatched_files: true
