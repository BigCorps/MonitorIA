name: Deprecated RTSP Sampler (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Sampler retired in 1.0.2
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired workflow
        run: |
          echo "The experimental RTSP sampler workflow was retired in MonitorIA 1.0.2."
          echo "RTSP sampling is part of the official Agent Core. Nothing is built or published here."
