# RETIRADO NA 1.0.2.
#
# Este arquivo-túmulo existe apenas para que um upload único pelo GitHub Web
# substitua o workflow experimental que já está na main. Sem `name`, `on` ou
# `jobs`, o GitHub Actions não o registra nem permite executá-lo.
# O sampler corrigido faz parte do código oficial do Agent 1.0.2.
