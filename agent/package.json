{
  "name": "@monitoria/agent",
  "version": "0.5.3",
  "private": true,
  "type": "module",
  "description": "MonitorIA Agent local para câmeras RTSP.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "check": "tsc --noEmit -p ./tsconfig.json",
    "build:js": "tsc -p ./tsconfig.json",
    "build:win": "bun build ./src/index.ts --compile --target=bun-windows-x64-baseline --outfile ./dist/monitoria-agent.exe"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
